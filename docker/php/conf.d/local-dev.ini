; Local development only. The base image's 128M default is too low for
; Larastan/PHPStan's parallel workers (crashes with "reached configured
; PHP memory limit") — see docs/handoffs/V1_PHASE_04A_HANDOFF.md.
memory_limit = 512M
